extends layout

block content
  h1= title
  p Keerthi Muli #{title}
  button(type='button' class=`btn btn-secondary` onclick = 'update()' ) Press me

  div#target Result will be displayed here
  - var url  = 'park';
  a(href='/' + url) The parks
  script.
    var client_count = 0
    function update(){
      client_count++
      console.log(client_count)
      document.getElementById("target").innerHTML = "Value now " +client_count;
    }
